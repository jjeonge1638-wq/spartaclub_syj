# 활주로 건설
T = int(input())

for tc in range(1, T+1):
    N, X = map(int, input().split())
    N = [list(map(int, input().split())) for _ in range(N)]

    for i in range(N):
        for j in range(N):
            # 행 우선 탐색
            # 카운트
            # 열 우선 탐색
            # 카운트

            #
